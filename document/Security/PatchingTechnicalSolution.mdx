# Patching Technical Solution
The RODE platform software is in a state of continued development and updates.

RODE uses the native kubernetes deployment rollouts and version history for managing rollbacks.

These deployments and container images are generated by the Amazon Web Services (AWS) Code Pipeline. The code pipeline for each service will be triggered weekly. 

The Code Pipline stages (in execution order) are:
| Stage | Purpose |
| :--- | :--- |
| | |